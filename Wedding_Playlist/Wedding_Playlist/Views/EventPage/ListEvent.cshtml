@model IEnumerable<Wedding_Playlist.Models.EventDTO>
@{
    ViewData["Title"] = "Events";
}
<h1 class="page-title">Events</h1>

<div class="button-container">
    <a asp-action="Create" class="btn btn-primary">Create New Event</a>
</div>

<div class="table-container">
    <table class="styled-table">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Name)</th>
                <th>@Html.DisplayNameFor(model => model.Date)</th>
                <th>@Html.DisplayNameFor(model => model.Location)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Name)</td>
                    <td>@item.Date.ToString("MMM dd, yyyy")</td>
                    <td>@Html.DisplayFor(modelItem => item.Location)</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.EventId" class="btn btn-warning btn-sm">✏️</a>
                        <a asp-action="Delete" asp-route-id="@item.EventId" class="btn btn-danger btn-sm">🗑</a>
                        <a asp-action="Details" asp-route-id="@item.EventId" class="btn btn-info btn-sm">Details</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
